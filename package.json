{
  "name": "menu-talent-site",
  "private": true,
  "version": "1.0.0",
  "description": "Local preview helpers for the Jekyll-based talent site.",
  "scripts": {
    "generate:jobs": "node scripts/generate_job_pages.js",
    "preview": "./scripts/jekyll.sh serve --host 127.0.0.1 --port 4000",
    "build": "./scripts/jekyll.sh build",
    "doctor": "./scripts/jekyll.sh doctor"
  }
}
